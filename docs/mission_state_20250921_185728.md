# Mission State Checkpoint - 2025-09-21 18:57:28 +03

## Entry Points & Scope
- `ui.ts:876` and `ui.ts:1589` Mermaid rendering flows for live conversation bubbles.
- `selectionSensei.ts:538` modal `processMermaidDiagrams` pathway handling selection pop-up diagrams.
- `mermaidErrorRecovery.ts:232` LLM fix routines and new `runMermaidRecovery` orchestrator.
- `mermaidManager.ts:326` rendering gateway producing errors consumed by recovery.
- `mermaid-theme-integration.js:15` thumbnail replacement for rendered diagrams.
- `logger.ts:1` logging infrastructure for validation messages.

## Static Execution Trace
1. Selection modal calls `processMermaidDiagrams` → `mermaidManager.render` → success or throw.
2. On failure we plan to delegate to `runMermaidRecovery` (-> `attemptMermaidFix` → Gemini interaction → diagram string).
3. Successful render pipelines to `renderMermaidThumbnailWithTheme` and updates modal DOM.
4. Exhaustion triggers fallback `[Diagram could not be rendered]` block as currently implemented.

## Dependency & Side-Effect Findings
| Function | Dependencies | Side Effects |
| --- | --- | --- |
| processMermaidDiagrams (`selectionSensei.ts:538`) | mermaidManager.render, renderMermaidThumbnailWithTheme, logger, DEBUG_FLAGS | Mutates modal content; currently spawns Promise.all parallel renders. |
| runMermaidRecovery (`mermaidErrorRecovery.ts:232`) | applyBacktickFix, applyUniversalQuoteFix, attemptMermaidFix, logger | Performs sequential retries and returns rendered SVG data. |
| attemptMermaidFix (`mermaidErrorRecovery.ts:232`) | fixSubgraphDirections, ai.models.generateContent | Calls Gemini Flash; returns diagram text. |
| mermaidManager.render (`mermaidManager.ts:326`) | initializeMermaid | Throws on syntax errors; returns `{ svg }`. |
| renderMermaidThumbnailWithTheme (`mermaid-theme-integration.js:15`) | DOMParser, logger | Replaces nodes and attaches lightbox handlers. |

## Key Architectural Insights
- SelectionSensei runs renders in parallel via `Promise.all`; integrating recovery needs sequential handling per block to honor latest diagram/error feedback.
- We must maintain UI feedback (spinner/error div) similar to conversation path for consistency.
- AI instance for modal resides on class (`this.ai`), so recovery helper must accept that reference rather than `window.ai`.

## Next Protocol
- Continue **MANDATORY PRINCIPLE-DRIVEN FEATURE IMPLEMENTATION PROTOCOL** (Step 7 extension)
