# Mission State Checkpoint - 2025-09-19 02:30:06

## Entry Points & Scope
- `index.tsx:415` `generateNextSenseiResponse` orchestrates each teaching turn; this is the operational entry point for chunk delivery length.
- Supporting scope includes: `index.tsx:366` `calculateFocusStrategy`, `curriculum.ts:1180` `getCurriculumFocusInstruction`, `curriculum.ts:1103` `buildPrimaryActionInstruction`, `prompts.ts:1006` `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE`, `interactionHelpers.ts:62` `buildSenseiDynamicSystemInstruction`, `interactionHelpers.ts:121` `streamMainSenseiResponse`, `model_usage.ts:33` `MAIN_SENSEI_RESPONSE_CHAT_MODEL_CONFIG`, and `geminiService.ts:1` for context on LLM planning inputs.

## Static Execution Trace
1. `handleUserInput` (index.tsx:700) → delegates to `generateNextSenseiResponse`.
2. `generateNextSenseiResponse` ensures teaching plan via `ensureTeachingPlanExists` then computes focus with `calculateFocusStrategy` → `calculateFocusPoints` (curriculum.ts:1189).
3. `generateNextSenseiResponse` calls `getCurriculumFocusInstruction` → `buildPrimaryActionInstruction` → `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE` to build chunk directives.
4. `buildSenseiDynamicSystemInstruction` wraps the curriculum focus using `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION`.
5. `streamMainSenseiResponse` sends the instruction to Gemini Pro with configuration from `MAIN_SENSEI_RESPONSE_CHAT_MODEL_CONFIG`, producing the learner-facing teaching text.

## Dependency & Side-Effect Summary
| Function | Key Dependencies | Major Side Effects |
| --- | --- | --- |
| `handleUserInput` | DOM inputs, `generateNextSenseiResponse`, `displayMessage` | Updates UI, logs user input history |
| `generateNextSenseiResponse` | `ensureTeachingPlanExists`, `getAnalysisFromGemini`, `updateLearnerModel`, `calculateFocusStrategy`, `profiler.getDirective`, `getCurriculumFocusInstruction`, `buildSenseiDynamicSystemInstruction`, `streamMainSenseiResponse` | Mutates learner model, curriculum state, issues LLM calls, streams UI response |
| `calculateFocusStrategy` | `calculateFocusPoints` | None (pure data shaping) |
| `calculateFocusPoints` | `curriculumState` teaching plan | Reads and filters teaching points; no direct mutation |
| `buildPrimaryActionInstruction` | Prompt templates in `prompts.ts` | Returns chunk instruction string |
| `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE` | Focus point list | Generates imperative prompt text; no mutation |
| `buildSenseiDynamicSystemInstruction` | `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION` | Constructs dynamic system message string |
| `streamMainSenseiResponse` | Gemini chat API, `updateMessageStream` | Triggers network call to Gemini, streams output to UI |
| `MAIN_SENSEI_RESPONSE_CHAT_MODEL_CONFIG` | Model name/params | Defines runtime limits (temperature, token behavior) for main chat |

## Key Architectural Insights
- Teaching depth is primarily governed by prompt templates inside `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE`, but real output length is constrained by chat model configuration and overall dynamic system instruction.
- No explicit `maxOutputTokens` is set in `MAIN_SENSEI_RESPONSE_CHAT_MODEL_CONFIG`, implying reliance on Gemini defaults, which likely caps response length and may undercut “double length” directives.
- Curriculum focus instructions already demand high thoroughness, suggesting adjustments must target enforceable constraints (token limits, explicit length directives, or structured output checks) rather than generic wording.

## Next Protocol
Initiate the **MANDATORY PRINCIPLE-DRIVEN FEATURE IMPLEMENTATION PROTOCOL** to design and approve the approach for increasing teaching output length.
