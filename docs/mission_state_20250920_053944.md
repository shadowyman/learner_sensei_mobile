# Mission State — 2025-09-20 05:39:44 UTC

## Parallel Analysis Agents
- Agent A (Flow Orchestrator): `ui.ts` `displayMessage` sensei branch, mermaid block detection, DOM streaming stores.
- Agent B (Markdown Synthesizer): `ui.ts` markdown sanitization, `marked.parse`, highlight pipeline for mermaid `<pre><code>` blocks.
- Agent C (Thumbnail Integrator): `mermaid-theme-integration.js` `renderMermaidThumbnailWithTheme` DOMParser, figure wrapping, annotation relocation, lightbox listener.
- Agent D (Styling Envelope): `index.css` `.message-bubble`, `.markdown-content p`, `.mermaid-thumbnail` width rules controlling layout context.
- Agent E (Prompt Policy): `prompts.ts` `MERMAID_GENERATION_GUIDELINES` enforcing italic caption semantics beneath diagrams.

## Scope & Entry Points
- Primary entry: `displayMessage(message)` in `ui.ts` when `message.sender === 'sensei'` and markdown contains ```mermaid``` blocks with following italic annotations.
- Supporting modules: `mermaidManager.ts` (`render`, `initializeMermaid`) for SVG generation; `mermaid-theme-integration.js` for DOM replacement; `index.css` for bubble and mermaid styling; `prompts.ts` ensuring captions remain italic paragraphs.

## Static Execution Trace
1. `displayMessage` (sensei branch) -> sanitize via `sanitizeCodeFences` -> render markdown through `marked.parse` into `.message-text`.
2. `displayMessage` -> iterate `pre code.language-mermaid` -> `mermaidManager.render` to obtain SVG markup and theme metadata.
3. `displayMessage` -> `renderMermaidThumbnailWithTheme(preElement, svg, currentTheme, rawMermaidCode)`.
4. `renderMermaidThumbnailWithTheme` -> parse SVG -> assign orientation classes -> wrap thumbnail inside `.mermaid-figure` container -> relocate adjacent italic `<p>` into figure as `.mermaid-annotation`.
5. `renderMermaidThumbnailWithTheme` -> attach lightbox click handler preserving theme + raw code for re-render.

## Dependency & Side-Effect Table
| Function | Key Dependencies | Side Effects |
| --- | --- | --- |
| `displayMessage` (`ui.ts`) | `sanitizeCodeFences`, `marked.parse`, `mermaidManager.render`, `mermaidManager.getCurrentTheme`, `renderMermaidThumbnailWithTheme`, `logger`, `hljs` | Mutates DOM bubbles, updates `streamingMessagesRawText`, schedules timers, replaces `<pre>` nodes with thumbnails, logs mermaid activity |
| `mermaidManager.render` (`mermaidManager.ts`) | `initializeMermaid`, `mermaidInstance.render`, `MERMAID_THEMES` | Lazily initializes Mermaid engine, persists theme preference, returns SVG |
| `initializeMermaid` (`mermaidManager.ts`) | Dynamic import `mermaid`, theme configs | Configures global mermaid instance, logs, sets `isInitialized` flag |
| `renderMermaidThumbnailWithTheme` (`mermaid-theme-integration.js`) | `window.mermaidManager`, `window.updateMermaidThemeClass`, `DOMParser`, `logger` | Replaces `<pre>` with themed thumbnail, injects `.mermaid-figure`, moves italic caption under diagram, registers lightbox click listener |

## Key Insights
- Annotation relocation depends on italic `<p>` immediately following the diagram block; resulting `.mermaid-figure` inherits thumbnail width modifiers like `.mermaid-thumbnail--horizontal`.
- Bubble bodies (`.message-bubble`, `.markdown-content`) retain full-width text flow, so caption width relies entirely on `.mermaid-figure` styling.
- Current `index.css` lacks explicit rules for `.mermaid-figure`/`.mermaid-annotation`, so width and stacking default to thumbnail classes, potentially causing overlap with subsequent instruction paragraphs when widths diverge.
- Prompt contract still mandates italic multi-sentence caption, so regression likely stems from presentation layer rather than content generation.

## Next Protocol
- Proceed with **MANDATORY ADAPTIVE ROOT CAUSE ANALYSIS & REMEDIATION PROTOCOL** to isolate the overlap defect.
