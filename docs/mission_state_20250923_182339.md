# Mission State Checkpoint — 2025-09-23 18:16:00

## Scope & Entry Points
- Entry: `buildSenseiDynamicSystemInstruction` (interactionHelpers.ts:62) when composing Sensei's dynamic prompt for standard turns.
- Supporting generators: `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION` (prompts.ts:363) and `getCurriculumFocusInstruction` (curriculum.ts:1187) which resolve the curriculum focus payload.
- Subcomponents: `buildContextualInstruction`, `buildPrimaryActionInstruction`, and related constants under `prompts.ts` and `curriculum.ts` that shape section order and headings.

## Static Execution Trace
1. `generateNextSenseiResponse` (index.tsx:480) → obtains curriculum focus via `getCurriculumFocusInstruction`.
2. `getCurriculumFocusInstruction` → `buildContextualInstruction` → `buildPrimaryActionInstruction` (curriculum.ts:1081, 1120) assembling focus text (module details, primary action, supporting context, IntroIllustrate expansion, check-your-understanding).
3. `buildSenseiDynamicSystemInstruction` (interactionHelpers.ts:62) calls `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION` with curriculum focus string and pedagogical guidance.
4. `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION` (prompts.ts:363) wraps combined content in the legacy "Task & Checklist" scaffolding and injects `PedagogicalGuidance` / `CurriculumFocus` strings.
5. Resulting prompt is streamed by `streamMainSenseiResponse` → forwarded to Gemini chat.

## Dependency & Side-Effect Table
| Function | Key Dependencies | Side Effects |
| --- | --- | --- |
| `buildSenseiDynamicSystemInstruction` (interactionHelpers.ts:62) | `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION`, navigation context, pedagogical guidance | None (returns string for LLM system prompt) |
| `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION` (prompts.ts:363) | Curriculum focus text, optional guidance | Formats master prompt string (no state) |
| `getCurriculumFocusInstruction` (curriculum.ts:1187) | `buildContextualInstruction`, focus points, KC metadata | Returns curriculum focus text; no side effects |
| `buildContextualInstruction` (curriculum.ts:1120) | `CURRICULUM_FOCUS_*` constants, primary action text, IntroIllustrate expansion | Concatenates multi-section string; references global constants |
| `buildPrimaryActionInstruction` (curriculum.ts:1081) | Teaching point text, phase | Returns per-action instructions; currently prefixes IntroIllustrate dual-pass block |

## Key Insights
- Legacy "RecursiveSensei Task & Checklist" scaffold is emitted inside `MAIN_SENSEI_RESPONSE_SYSTEM_INSTRUCTION_TEMPLATE_FUNCTION`; restructuring must occur here for the top-level execution directive and section separators.
- Curriculum focus string (`buildContextualInstruction`) currently emits bracketed headers, primary action banner, supporting context, and IntroIllustrate expansion. We must realign this to the new heading/`======` format and adjust where the dual-pass section lives.
- Teaching points label and supporting-context heading are hard-coded inside `buildContextualInstruction`; will need renaming to "Teaching Points" and "SUPPORTING CONTEXT & GUIDANCE FOR YOUR REFERENCE" respectively.
- "Check Your Understanding" section is appended via `buildPrimaryActionInstruction` (when `shouldIncludeCheckUnderstanding` true) and via contextual instruction; relocation requires moving this snippet out of templates into structural composition to match the new order.
- Pedagogical guidance text currently merged by template (`MAIN_SENSEI...`) as inline bullet block; must be repositioned after "Structure of Teaching Format" per prototype.

## Triggering Protocol
Prepared to execute **Mandatory Principle-Driven Feature Implementation Protocol** using this checkpoint.
