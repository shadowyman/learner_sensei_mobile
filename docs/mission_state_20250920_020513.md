# Mission State Checkpoint (2025-09-20 02:05:13)

## Scope and Entry Points
- `ui.ts`: `displayMessage` constructs the live response bubble, spinner, and timer for Sensei output.
- `index.tsx`: `generateNextSenseiResponse` prepares the Sensei loading message that triggers the timer UI.
- `debugMode.ts`: `handleSendToGemini` and `displayDebugMessage` mirror the loading UI for debugger streams.
- `index.css`: `.inline-timer` styles the inline timer element rendered beside the spinner.

## Static Execution Trace
1. `generateNextSenseiResponse` (index.tsx:425) creates a Sensei message with `isLoading: true` and calls `displayMessage`.
2. `displayMessage` (ui.ts:751) renders the loading bubble, appends spinner and timer span, and starts a 1s interval updating the timer text.
3. `displayMessage` (ui.ts:751) runs again with `isLoading: false` after streaming to replace the loading UI and clear the timer.
4. `handleSendToGemini` (debugMode.ts:520) issues a debugger request, calling `displayDebugMessage` with `isLoading: true` for Gemini output.
5. `displayDebugMessage` (debugMode.ts:365) mirrors the spinner/timer creation and interval management for debugger flows.

## Dependency and Side-Effect Analysis
| Function Name | Dependencies | Side Effects |
| --- | --- | --- |
| `generateNextSenseiResponse` (index.tsx) | `displayMessage`, `streamMainSenseiResponse`, `showLoading`, curriculum helpers, learner model state | Pushes loading message to UI, updates global message IDs, toggles loading overlay, mutates learner model and curriculum state |
| `displayMessage` (ui.ts) | DOM API, `streamingMessageTimers`, `streamingMessagesRawText`, `marked`, curriculum helpers | Manipulates chat DOM, starts/clears timer intervals, updates streaming maps, triggers syntax highlighting |
| `handleSendToGemini` (debugMode.ts) | `displayDebugMessage`, Gemini client, DOM refs for debug UI | Disables/enables debugger controls, posts user/debugger messages, orchestrates debug streaming lifecycle |
| `displayDebugMessage` (debugMode.ts) | DOM API, `debugStreamingMessageTimers`, `debugStreamingMessagesRawText`, `marked`, `hljs` | Updates debugger chat DOM, sets/clears timer intervals, stores raw streaming text, applies syntax highlighting |

## Architectural Insights
- Chat UI uses imperative DOM updates with shared timer registries to prevent orphaned intervals.
- Loading indicators rely on paired `isLoading` states; both main and debug paths share the spinner-timer pattern.
- Styling for timers is centralized via `.inline-timer`, so text additions must respect existing layout constraints.

## Next Protocol
- Proceed with the **MANDATORY PRINCIPLE-DRIVEN FEATURE IMPLEMENTATION PROTOCOL** using this checkpoint as context.
