# Mission State Checkpoint (${timestamp})

## Analysis Scope & Entry Points
- Focus: Technical drilldown instructions emitted during IntroIllustrate "Teach New Content" turns.
- Primary code paths: `curriculum.ts:1113` `buildPrimaryActionInstruction` and `prompts.ts:881` `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE`.
- Supporting context: `curriculum.ts:1268` `getCurriculumFocusInstructionImpl`, `curriculum.ts:1139` `buildContextualInstruction`, and `prompts.ts:808` `CURRICULUM_FOCUS_EXECUTION_DIRECTIVE_BODY`.

## Static Execution Trace
1. `getCurriculumFocusInstruction` → `getCurriculumFocusInstructionImpl`
2. `getCurriculumFocusInstructionImpl` → `buildEarlyReturnInstruction` (optional) → `resolveFocusPoints` → `buildPrimaryActionInstruction`
3. `buildPrimaryActionInstruction` (Teach New Content case) → `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE`
4. `buildPrimaryActionInstruction` returns string → `buildContextualInstruction`
5. `buildContextualInstruction` appends `CURRICULUM_FOCUS_EXECUTION_DIRECTIVE_BODY` → produces final instruction block

## Dependency & Side-Effect Table
| Function | Dependencies | Side Effects |
| --- | --- | --- |
| `getCurriculumFocusInstructionImpl` | Calls `buildEarlyReturnInstruction`, `resolveFocusPoints`, `buildPrimaryActionInstruction`, `buildContextualInstruction`; reads `CurriculumState` fields | None (pure string assembly) |
| `buildPrimaryActionInstruction` | Uses `REVISIT_*` templates, `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE`, `DEBUG_FLAGS.curriculum_debug`, `logger` | Logs debug message when IntroIllustrate debug flag is set |
| `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE` | Receives focus points and intro expansion text; embedded in prompts | None (string generation) |
| `buildContextualInstruction` | Concatenates header constants and `CURRICULUM_FOCUS_EXECUTION_DIRECTIVE_BODY`; conditionally logs via `logger` | Logs when Intro phase debug flag active |
| `CURRICULUM_FOCUS_EXECUTION_DIRECTIVE_BODY` | Static directive string reused across prompts | None |

## Key Architectural Insights
- Technical drilldown guidance is duplicated: once in `TEACH_NEW_CONTENT_CHUNK_PROMPT_TEMPLATE` via Intro expansion, and once globally in `CURRICULUM_FOCUS_EXECUTION_DIRECTIVE_BODY`.
- Decisions about including the drilldown text hinge on `buildPrimaryActionInstruction` when `primaryActionType` is "Teach New Content" and `state.currentPhase === 'IntroIllustrate'`.
- Adjustments must coordinate both the intro expansion block and the global directive to avoid conflicting instructions.

## Next Protocol
- Triggering: **MANDATORY ARCHITECTURAL SYNTHESIS PROTOCOL** (to redesign drilldown toolkit selection per new requirements).

Core analysis complete. I have mapped the execution trace and identified all dependencies and side effects. I am now ready to proceed with the MANDATORY ARCHITECTURAL SYNTHESIS PROTOCOL.
