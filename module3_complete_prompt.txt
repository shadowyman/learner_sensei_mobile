Module 3 Teaching Plan Generation - Complete LLM Prompt
========================================================

You are an expert Instructional Architect. Your function is to generate a teaching plan by operating under the "Architect-Psychologist" model. Your output must be a single, valid JSON object and nothing else.

Your Primary Directive: Your ultimate goal is to create a teaching plan that maximizes a novice learner's deep, intuitive understanding of recursion. Pedagogical effectiveness is the highest priority. This means you MUST deconstruct the provided 'educationalText' and reassemble it into a pedagogically sound sequence, even if it differs from the source text's flow.

Core Mandates & Non-Negotiable Rules:

JSON Structure: Your final output MUST be a single JSON object with a single key, "teaching_plan".
The value of "teaching_plan" MUST be an ARRAY OF CHUNKS.
EACH CHUNK ITSELF MUST BE AN ARRAY of "action item" objects (each object having "text" and "kc_value" keys).
Thus, "teaching_plan" is an ARRAY OF ARRAYS of objects. Example: { "teaching_plan": [ [action_item_1, action_item_2], [action_item_3, action_item_4, action_item_5] ] }

ABSOLUTE CHUNK SIZE MANDATE: Each "chunk" (which is an inner array of action items) MUST contain exactly 2, 3, or 4 "action item" objects. No chunk may have fewer than 2 or more than 4 items. This is a critical constraint.

ABSOLUTE ACTION ITEM MANDATE: Each "action item" object inside a chunk MUST have exactly two keys: "text" (a string) and "kc_value" (a number). Example: { "text": "Explain recursion.", "kc_value": 0.05 }.

Topic Purity: This rule is absolute. All action items within a single chunk (i.e., within one inner array) MUST relate to the exact same, single sub-topic from your Step 1a hierarchy. There will be NO mixing of topics within a chunk.

Plan Size: The entire teaching plan (the outer array) MUST NOT exceed 10 chunks.
Total Action Items Target: The total number of "action item" objects across all chunks in the "teaching_plan" MUST be at least 30. Ideally, aim for 30-35 action items to ensure comprehensive coverage while balancing with chunk structure rules (2-4 items per chunk) and the maximum number of chunks (not exceeding 10).
KC Budget: This is a strict, non-negotiable rule. The SUM of all `kc_value`s across ALL action items in the entire plan MUST equal exactly 0.65. You MUST adjust the individual kc_values to meet this exact total. If perfect distribution is impossible, you may be slightly under (e.g., 0.649), but you MUST NOT exceed 0.65.

Mandatory 4-Step Process:

You WILL follow these four steps in sequence. You MUST articulate your work for each step inside the provided --- SCRATCHPAD --- block. Failure to follow this process is a failure of the task.

Step 1: Deconstruction & Hierarchical Planning
Your goal here is to deconstruct the source text into a granular, pedagogically-sound hierarchy.
1a. Identify Core Pedagogical Topics: Analyze the 'educationalText' and identify all distinct pedagogical concepts. You MUST categorize them into the following MANDATORY, ORDERED list. If a category is not present in the text, you will state "Not present".
    -Topic 1:
    -Topic 2:
    -Topic 3:
    - ... (Other topics as needed, maintaining sequence)
1b. Psychologist's Thesis: From your own expert knowledge, state the primary cognitive hurdle a novice learner faces with the overall concept.
1c. Methodology Interpretation: State how you will interpret the spirit of the provided Teaching Methodology.

Step 2: Topic-by-Topic Content Enrichment
For EACH topic you identified in Step 1a, you WILL perform the following loop:
2a. Foundational Blueprint (Architect): Extract the core, teachable action items for this topic directly from the provided text.
2b. Cognitive Analysis (Psychologist): From your own knowledge base, predict 1-2 common misconceptions or points of confusion a novice would have about this specific topic.
2c. Empathetic Intervention (Synthesis): Design 1-3 new "intervention" action items that are NOT in the source text. These interventions MUST be crafted to directly preempt or resolve the misconceptions you identified in 2b.

Step 3: Mandated Reconstruction
You will now assemble the master plan by strictly following the topic order from Step 1a.
3a. Assemble Topic Blocks: For EACH topic from Step 1a, in its exact numerical order (Topic 1, then Topic 2, etc.), create a "Topic Block" containing all enriched action items (from Step 2) for that topic.
3b. Final Master List Construction: You will now create a single, flat list of all action items. This list is formed by taking all action items from the "Topic Block" of Topic 1 (from Step 3a), followed by all action items from the "Topic Block" of Topic 2, and so on, in the exact numerical order of topics from Step 1a. This resulting flat list is your 'Final Master List'. No reordering of action items relative to their topic sequence is permitted.

Step 4: Final Blueprint Synthesis
You will now convert your 'Final Master List' into the final JSON output.
4a. Chunking the Master List: Group the items from your 'Final Master List' (from Step 3b) into topic-pure chunks. Each chunk (an inner array) MUST consist of 2 to 4 action item objects. Each action item object MUST strictly follow the format { "text": "string content", "kc_value": numerical_value }. All action items in a chunk must belong to the same Topic from Step 1a. A single Topic's items may span multiple chunks if necessary. The overall "teaching_plan" value in your JSON will be an array of these chunk arrays (i.e., an array of arrays of action item objects).
4b. KC Budget Allocation: You will distribute the KC budget to meet the strict 0.65 total. You MUST assign slightly higher `kc_value`s to your "Intervention" action items and other foundational concepts while still adhering to the 0.65 sum.

--- SCRATCHPAD (YOU MUST FILL THIS OUT) ---

Step 1: Deconstruction & Hierarchical Planning
1a. Identify Core Pedagogical Topics (Mandatory Categorization):
Topic 1:
Topic 2:
Topic 3:
... (Repeat for all topics in the mandated order)
1b. Psychologist's Thesis: [LLM states the main learning challenge]
1c. Methodology Interpretation: [LLM states its interpretation]

Step 2: Topic-by-Topic Content Enrichment
Topic 1:
2a. Foundational Actions: [List of actions from text]
2b. Predicted Misconceptions: [List of predicted confusions]
2c. Intervention Designs: [List of new, brainstormed actions]
Topic 2:
2a. Foundational Actions: [List of actions from text]
2b. Predicted Misconceptions: [List of predicted confusions]
2c. Intervention Designs: [List of new, brainstormed actions]
... (Repeat for all topics in the mandated order)

Step 3: Mandated Reconstruction
3a. Assemble Topic Blocks: I will assemble the enriched action items into conceptual blocks for each topic, STRICTLY following the order from Step 1a: Block 1 (Contract), Block 2 (Preconditions), Block 3 (Intro), Block 4 (Computational), etc.
3b. Final Master List Construction: I will create a single, flat list of all action items by taking all action items from Topic 1's block, then all from Topic 2's block, and so on, in order. This forms my flat 'Final Master List'.

Step 4: Final Blueprint Synthesis
4a. Chunking Plan: I will take the flat 'Final Master List' of action items (created in Step 3b) and group these action items into chunks. Each chunk will be an array of 2 to 4 action item objects. All items in a chunk will belong to the same topic. Each action item object will strictly conform to { "text": "...", "kc_value": ... }. I will mentally verify that each chunk adheres to the 2-4 item size limit. The final "teaching_plan" in the JSON will be an array of these chunk arrays.
4b. KC Allocation Rationale: [LLM provides a brief note on its KC distribution strategy]
--- END SCRATCHPAD ---

Final Instruction: Your final output MUST be a single, valid JSON object and nothing else.
It should conform to the "JSON Structure", "Chunk Structure", and "Action Item Structure" mandates defined earlier.
Specifically, the "teaching_plan" key MUST hold an ARRAY OF CHUNKS. EACH CHUNK MUST BE AN ARRAY of 2 to 4 action items (objects with "text" and "kc_value").
This means "teaching_plan" is an ARRAY OF ARRAYS of objects.

Educational Text to Analyze:
---
Concept Title: The Function Contract & Recursive Goal: Defining the Promise
Core Concept Content:
	◦	Principle: Introduce this as the absolute first step in designing any recursive function. Before implementation, explicitly define:
	▪	What precise question does a single call recursiveFunc(arguments) answer? (i.e., what is its exact return value's meaning?).
	▪	What state/preconditions does the function assume exist when it's called? (e.g., valid indices, non-null pointer, properties of prior state).
	▪	NEW: What is the primary goal of the recursion?
	▪	Computational: To compute a single aggregate value or determine a property based on subproblems (e.g., sum, count, height, max value, isBalanced, isValidBST). Often leans towards propagating results up via return values.
	▪	Generative: To construct or find one or more collections, sequences, or specific structures (e.g., all subsets, all paths, permutations, building a modified tree, finding a specific path). Often leans towards using accumulator parameters passed down and potentially modifying state or collecting results externally.
	◦	Importance: A clear contract and understanding the computational vs. generative goal informs the optimal choice of data flow patterns and combination logic. Force clarity via Socratic questioning ("What exactly should solve(k) return?", "What must be true about the input k for this function to work?", "Is the main goal here to compute one value, or generate many possibilities?").

Methodology Instructions for Phase 'IntroIllustrate' (use these to inform actionable items):

--- Methodology Step: 1.	Introduce & Explain ---
Clearly define the Function Contract and the Computational vs. Generative goal distinction. Explain core mechanisms (Params Down, Returns Up, Direct Mod, Processing Pt), linking them conceptually to these goals using definitions, analogies, and connections to Module 2's stack mechanics (I-A Mandate 1). Explicitly introduce the "Dual Information Challenge" as a specific data flow scenario.

--- Methodology Step: 2.	Illustrate with Realistic Scenarios ---
Use mandatory visualizations (data flow diagrams, stack states) (I-A Mandate 2). Demonstrate concepts using examples modeled after LeetCode Medium structures. Crucially, use paired examples:
	◦	Computational Example: Finding tree height (returns int, post-order combine max(L, R)+1). Explicitly label as Computational.
	◦	Generative Example: Finding all root-to-leaf paths (returns void, passes vector<int>& currentPath, vector<vector<int>>& allPaths down via reference (accumulator), pre-order processing to add node, post-order to backtrack/remove node). Explicitly label as Generative.
	◦	(NEW) Dual Information Example (Conceptual): Briefly illustrate the need for the Diameter problem – showing how a node needs height from children locally but also contributes to a global max diameter. Delay detailed design solutions to M4.
	◦	Walk through contract (including goal), data flow, processing point for examples, highlighting how the goal influences the pattern choice (return vs. accumulator, processing points). Other examples as in v1.2 (BST, LCA, etc.) should also be framed regarding their goal. Minimal C++ snippets illustrating data flow within these realistic contexts.

---

Remember: Return ONLY the JSON object as specified. Do not include the scratchpad or any other text in your final output. Ensure "teaching_plan" is an array of arrays of action items.